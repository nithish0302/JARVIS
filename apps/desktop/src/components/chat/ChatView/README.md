# ChatView

Page-level composition for the chat domain. It decides whether to render the `IdleView` or the `ConversationArea`. Currently uses a temporary wrapper pattern with mock static data for milestone validation.

## Usage

```tsx
import { ChatView } from "./ChatView";

<ChatView />
```
