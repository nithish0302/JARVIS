# StreamingMessage

A component that renders the active streaming response from JARVIS. It displays the text as it is generated token-by-token and includes a pulsing cursor to indicate that JARVIS is currently thinking/typing.
