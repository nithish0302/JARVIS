# MessageList

Presentation component that maps an array of messages to `MessageBubble` instances. Handles empty states by rendering nothing.

## Usage

```tsx
import { MessageList } from "./MessageList";

<MessageList messages={messagesArray} />
```
