# ActionFeedback

A floating feedback component that displays UI action execution messages near the Orb.

## Responsibilities
- Renders `actionFeedback` from the `useAppStore`.
- Only visible when `actionFeedbackVisible` is true.
- Fades in and out using Framer Motion.
