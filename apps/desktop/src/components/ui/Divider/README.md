# Divider

`Divider` visually and semantically separates related content areas.

## Public API

- Horizontal-rule props, plus `orientation` (`horizontal`, `vertical`) and `className`.

## Accessibility

The horizontal form renders an `<hr>`. The vertical form renders an element with `role="separator"` and vertical orientation.

## Usage

```tsx
<Divider />
```
