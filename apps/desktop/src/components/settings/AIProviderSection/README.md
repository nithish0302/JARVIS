# AIProviderSection

`AIProviderSection` displays the settings related to configuring the underlying AI engine.

## Public API

- Takes no props.

## Accessibility

Composed using accessible `Select` and `Input` components.

## Usage

```tsx
<AIProviderSection />
```

## Used By

`SettingsView`
